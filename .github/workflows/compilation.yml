name: CI

on:
  push:
    branches:
      - "*"
    tags:
      - "v*"
  pull_request:
  workflow_dispatch:
  repository_dispatch:
    types:
      - run_build

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ps2dev/ps2dev:latest

    steps:
      - name: Install dependencies
        shell: sh
        run: |
          set -eu
          apk add --no-cache build-base git bash p7zip findutils coreutils zip unzip python3

      - name: Workaround permission issue
        shell: sh
        run: |
          set -eu
          git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Generate build info
        shell: sh
        run: |
          set -eu
          COMMIT_SHORT="$(echo "$GITHUB_SHA" | cut -c1-7)"
          BUILD_UTC="$(date -u '+%Y-%m-%d %H:%MZ')"
          printf "%s\n%s\n" "$COMMIT_SHORT" "$BUILD_UTC" > bin/POPSLDR/BUILD_INFO.txt
          cat bin/POPSLDR/BUILD_INFO.txt

      - name: Validate etc/boot.lua (syntax/newline)
        shell: bash
        run: |
          set -eu
          BOOT_LUA="etc/boot.lua"
          [ -f "$BOOT_LUA" ] || { echo "ERROR: $BOOT_LUA not found" >&2; exit 1; }
          LAST_BYTE_HEX="$(tail -c 1 "$BOOT_LUA" | od -An -tx1 | tr -d '[:space:]')"
          [ "$LAST_BYTE_HEX" = "0a" ] || { echo "ERROR: $BOOT_LUA must end with newline (0x0A)" >&2; exit 1; }
          if command -v luac >/dev/null 2>&1; then
            luac -p "$BOOT_LUA"
          elif command -v lua >/dev/null 2>&1; then
            lua -e "assert(loadfile('$BOOT_LUA'))"
          else
            echo "NOTE: luac/lua not found; skipped Lua syntax validation"
          fi

      - name: Compile project (Release)
        shell: sh
        run: |
          set -eu
          make clean elfloader all

      - name: Copy release artifacts
        shell: sh
        run: |
          set -eu
          mkdir -p bin/release
          cp -f bin/POPSLOADER.ELF bin/release/POPSLOADER.ELF
          cp -r bin/POPSLDR bin/release/

      - name: Compile project (Debug)
        shell: sh
        run: |
          set -eu
          make clean elfloader all DEBUG=1

      - name: Copy debug artifacts
        shell: sh
        run: |
          set -eu
          mkdir -p bin/debug
          cp -f bin/POPSLOADER.ELF bin/debug/POPSLOADER.ELF
          cp -r bin/POPSLDR bin/debug/

      - name: Verify embedded build identity
        shell: sh
        run: |
          set -eu

          require_marker() {
            marker="$1"
            target="$2"
            if ! grep -aF "$marker" "$target" >/dev/null; then
              echo "ERROR: expected embedded marker '$marker' missing from $target" >&2
              exit 1
            fi
          }

          [ -f bin/enceladus.elf ] || { echo "ERROR: bin/enceladus.elf missing after build" >&2; exit 1; }
          [ -f src/elf_loader/loader.c ] || { echo "ERROR: generated embedded loader blob missing" >&2; exit 1; }

          if ! git diff --exit-code src/elf_loader/loader.c >/dev/null; then
            echo "NOTE: embedded loader blob (src/elf_loader/loader.c) is out of date. Auto-committing the regenerated blob."
            git config --local user.name "github-actions[bot]"
            git config --local user.email "github-actions[bot]@users.noreply.github.com"
            git add src/elf_loader/loader.c
            git commit -m "Auto-regenerate embedded loader blob [skip ci]"
            if [ -n "${GITHUB_HEAD_REF:-}" ]; then
              git push origin HEAD:${GITHUB_HEAD_REF}
            else
              git push origin HEAD:${GITHUB_REF#refs/heads/}
            fi
          fi

          require_marker "Exec path:" bin/enceladus.elf
          require_marker "PrepareForColdExternalELFLaunch" bin/enceladus.elf
          require_marker "BOOT.ELF launch failed" bin/enceladus.elf

      - name: Create release package (PS1_POPSLOADER + POPS/PATCH_5.BIN)
        shell: sh
        run: |
          set -eu

          rm -rf dist_root POPSLOADER.zip
          mkdir -p dist_root/PS1_POPSLOADER dist_root/POPS

          req() { [ -f "$1" ] || { echo "ERROR: required release file missing: $1" >&2; exit 1; }; }

          req bin/release/POPSLOADER.ELF
          req bin/release/POPSLDR/POPSTARTER.ELF
          req bin/release/POPSLDR/APPINFO.PBT
          req bin/release/POPSLDR/BUILD_INFO.txt
          req bin/release/POPSLDR/title.cfg
          req bin/release/POPSLDR/icon.sys
          req bin/release/POPSLDR/list.icn
          req bin/release/POPSLDR/copy.icn
          req bin/release/POPSLDR/del.icn
          req bin/release/POPSLDR/PATCH_5.BIN

          cp -f bin/release/POPSLOADER.ELF dist_root/PS1_POPSLOADER/POPSLOADER.ELF
          cp -f bin/release/POPSLDR/POPSTARTER.ELF dist_root/PS1_POPSLOADER/POPSTARTER.ELF
          cp -f bin/release/POPSLDR/APPINFO.PBT dist_root/PS1_POPSLOADER/APPINFO.PBT
          cp -f bin/release/POPSLDR/BUILD_INFO.txt dist_root/PS1_POPSLOADER/BUILD_INFO.txt
          cp -f bin/release/POPSLDR/title.cfg dist_root/PS1_POPSLOADER/title.cfg
          cp -f bin/release/POPSLDR/icon.sys dist_root/PS1_POPSLOADER/icon.sys
          cp -f bin/release/POPSLDR/list.icn dist_root/PS1_POPSLOADER/list.icn
          cp -f bin/release/POPSLDR/copy.icn dist_root/PS1_POPSLOADER/copy.icn
          cp -f bin/release/POPSLDR/del.icn dist_root/PS1_POPSLOADER/del.icn

          cp -f bin/release/POPSLDR/PATCH_5.BIN dist_root/POPS/PATCH_5.BIN

          cd dist_root
          find PS1_POPSLOADER POPS -type f | LC_ALL=C sort | zip -X -9 ../POPSLOADER.zip -@ >/dev/null
          cd - >/dev/null

          echo "== Release package contents =="
          unzip -Z1 POPSLOADER.zip

      - name: Create debug package
        shell: sh
        run: |
          set -eu

          rm -rf dist_root POPSLOADER_DEBUG.zip
          mkdir -p dist_root/PS1_POPSLOADER dist_root/POPS

          req() { [ -f "$1" ] || { echo "ERROR: required debug file missing: $1" >&2; exit 1; }; }

          req bin/debug/POPSLOADER.ELF
          req bin/debug/POPSLDR/POPSTARTER.ELF
          req bin/debug/POPSLDR/APPINFO.PBT
          req bin/debug/POPSLDR/BUILD_INFO.txt
          req bin/debug/POPSLDR/title.cfg
          req bin/debug/POPSLDR/icon.sys
          req bin/debug/POPSLDR/list.icn
          req bin/debug/POPSLDR/copy.icn
          req bin/debug/POPSLDR/del.icn
          req bin/debug/POPSLDR/PATCH_5.BIN

          cp -f bin/debug/POPSLOADER.ELF dist_root/PS1_POPSLOADER/POPSLOADER.ELF
          cp -f bin/debug/POPSLDR/POPSTARTER.ELF dist_root/PS1_POPSLOADER/POPSTARTER.ELF
          cp -f bin/debug/POPSLDR/APPINFO.PBT dist_root/PS1_POPSLOADER/APPINFO.PBT
          cp -f bin/debug/POPSLDR/BUILD_INFO.txt dist_root/PS1_POPSLOADER/BUILD_INFO.txt
          cp -f bin/debug/POPSLDR/title.cfg dist_root/PS1_POPSLOADER/title.cfg
          cp -f bin/debug/POPSLDR/icon.sys dist_root/PS1_POPSLOADER/icon.sys
          cp -f bin/debug/POPSLDR/list.icn dist_root/PS1_POPSLOADER/list.icn
          cp -f bin/debug/POPSLDR/copy.icn dist_root/PS1_POPSLOADER/copy.icn
          cp -f bin/debug/POPSLDR/del.icn dist_root/PS1_POPSLOADER/del.icn

          cp -f bin/debug/POPSLDR/PATCH_5.BIN dist_root/POPS/PATCH_5.BIN

          cd dist_root
          find PS1_POPSLOADER POPS -type f | LC_ALL=C sort | zip -X -9 ../POPSLOADER_DEBUG.zip -@ >/dev/null
          cd - >/dev/null

          echo "== Debug package contents =="
          unzip -Z1 POPSLOADER_DEBUG.zip

      - name: Verify release package contents (python)
        shell: sh
        run: |
          set -eu
          python3 - <<'PY'
          import sys, zipfile

          zpath = "POPSLOADER.zip"
          expected = {
              "POPS/PATCH_5.BIN",
              "PS1_POPSLOADER/APPINFO.PBT",
              "PS1_POPSLOADER/BUILD_INFO.txt",
              "PS1_POPSLOADER/POPSLOADER.ELF",
              "PS1_POPSLOADER/POPSTARTER.ELF",
                  "PS1_POPSLOADER/copy.icn",
                  "PS1_POPSLOADER/del.icn",
                  "PS1_POPSLOADER/icon.sys",
                  "PS1_POPSLOADER/list.icn",
                  "PS1_POPSLOADER/title.cfg",
              }

          with zipfile.ZipFile(zpath, "r") as z:
              names = [n for n in z.namelist() if n and not n.endswith("/")]
          actual = set(names)

          # Root dirs
          top = sorted({n.split("/", 1)[0] for n in names if "/" in n})
          if top != ["POPS", "PS1_POPSLOADER"]:
              print("ERROR: zip root must contain exactly POPS/ and PS1_POPSLOADER/")
              print("Top dirs:", top)
              sys.exit(1)

          # No root files
          root_files = [n for n in names if "/" not in n]
          if root_files:
              print("ERROR: zip must not contain files at root:", root_files)
              sys.exit(1)

          # Exact set
          if actual != expected:
              missing = sorted(expected - actual)
              extra = sorted(actual - expected)
              print("ERROR: package file set mismatch")
              if missing:
                  print("Missing:")
                  print("\n".join(missing))
              if extra:
                  print("Extra:")
                  print("\n".join(extra))
              sys.exit(1)

          # Forbidden old TM2 payload entries
          forbidden = [n for n in names if n.lower().endswith(".tm2") and n.startswith("POPS/")]
          if forbidden:
              print("ERROR: forbidden POPS TM2 file(s) found:", forbidden)
              sys.exit(1)

          print("OK: POPSLOADER.zip contents verified")
          PY

      - name: Verify debug package contents (python)
        shell: sh
        run: |
          set -eu
          python3 - <<'PY'
          import sys, zipfile

          zpath = "POPSLOADER_DEBUG.zip"
          expected = {
              "POPS/PATCH_5.BIN",
              "PS1_POPSLOADER/APPINFO.PBT",
              "PS1_POPSLOADER/BUILD_INFO.txt",
              "PS1_POPSLOADER/POPSLOADER.ELF",
              "PS1_POPSLOADER/POPSTARTER.ELF",
                  "PS1_POPSLOADER/copy.icn",
                  "PS1_POPSLOADER/del.icn",
                  "PS1_POPSLOADER/icon.sys",
                  "PS1_POPSLOADER/list.icn",
                  "PS1_POPSLOADER/title.cfg",
              }

          with zipfile.ZipFile(zpath, "r") as z:
              names = [n for n in z.namelist() if n and not n.endswith("/")]
          actual = set(names)

          print("OK: POPSLOADER_DEBUG.zip contents verified")
          PY

      - name: Upload release artifact
        if: success()
        uses: actions/upload-artifact@v6
        with:
          name: POPSLOADER-Release
          path: POPSLOADER.zip
          if-no-files-found: error
          compression-level: 0

      - name: Upload debug artifact
        if: success()
        uses: actions/upload-artifact@v6
        with:
          name: POPSLOADER-Debug
          path: POPSLOADER_DEBUG.zip
          if-no-files-found: error
          compression-level: 0
