# sio2man variants

Place old `sio2man.irx` variants under:

```
sio2man/<variant>/sio2man.irx
```

The default build expects:

```
sio2man/sio2man/sio2man.irx
```

Use `make variants` to build per-variant POPSLOADER artifacts.
